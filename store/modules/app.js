import { setToken, getToken, removeToken, setLocal, getLocal, clearLocal } from '@/assets/utils/auth'
const app = {
  state: {
    locales: ['en', 'zh'],
    locale: 'zh'
  },
  mutations: {
  },
  actions: {
  }
}

export default app
